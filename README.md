This is the email_formatter repo.
This repo will host a tool that will be used to text stored in a document and format it into a Outlook compatible email. 
It will contain different headings, subheadings, fonts, etc to make the email more consumable. 
This format will be aimed towards End of Day reports.
